services:
  - type: web
    name: roulinpost-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    # Force Render to bundle native apt system packages
    buildFilter:
      packages:
        - ffmpeg